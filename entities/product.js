export class Product {
    constructor(product) {
        this.name = product.name;
        this.detail = product.detail;
        this.price = product.price;
        this.hero = product.hero;
        this.image = product.image;
    }
}